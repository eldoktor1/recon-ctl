# Research digest — tooling — 2026-08-29

# Research digest — tooling — 2026-08-29 (pass 2)

## Bottom line
Nothing cleared the bar this pass. Six targeted sweeps (JS-endpoint mining, web-cache deception, SSRF scanners, param discovery, nuclei-class exposure scanning, CT/subdomain monitoring, KEV/EPSS prioritization, kiterunner-successor) returned either: tools we've already evaluated in the last two weeks (WCVS, graphql-cop-adjacent territory), single-author/low-traction repos, or generic 2026 "bug bounty methodology" listicle content with no real tool behind it.

## Checked, rejected
- **`Sh1Yo/x8`** (Rust hidden-param discovery, pitched as an Arjun alternative) — 2.1k★ looks strong on paper, but `pushed_at` = **Sept 2024**. No commits in ~2 years — fails our "active in last 6-12 months" bar. Arjun stays.
- **`kston83/cvss-te`** (CVSS+KEV+EPSS+Metasploit blended severity scorer, surfaced as a possible n-day/KEV prioritizer) — 14★, single contributor, looks like a personal data-feed script rather than a maintained tool. Not worth wiring into `recon_nday.sh`.
- **SSRF scanner sweep** (`See-SURF`, `autossrf`, various single-repo "SSRF-to-RCE" tools) — all either AI-payload-guessing gimmicks, sub-50★ single-author releases, or (worse) tools that actively try to escalate SSRF→RCE — the opposite of our OOB-canary-only doctrine (`class-ssrf.md`). None beat our current interactsh-based confirm primitive.
- **Web-cache-deception tool sweep** — everything found is either a Burp extension (doesn't fit our headless CLI pipeline) or a low-star personal script; none add capability beyond Hackmanit's WCVS, which we already run.
- **Kiterunner-successor / "shadow API" search** — no successor exists; Kiterunner is still the field's tool of record as of 2026. No action.

## Recommendation
No tooling change this run. Given two straight quiet-ish passes (this one and diminishing returns on 08-23's H3 fuzzer), next `tooling` cycle should widen scope away from generic GitHub search terms — e.g. check Assetnote's/Project Discovery's own blogs directly, or specifically re-survey the JS-intel lane (jsluice/sourcemapper) since that's the one pillar lane not touched by a tooling digest in the last 30 days.
