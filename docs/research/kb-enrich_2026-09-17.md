# Research digest — kb-enrich — 2026-09-17

# Research digest — kb-enrich — 2026-09-17

## 1. Next.js — three new 2025/2026 CVEs deepen `tech-nextjs.md` (unauth-safe detection for two of three)

Our doc predates a middleware-bypass CVE that is now over a year old (patched, but a huge fraction of the estate won't be) plus a fresh unauth-RCE in image optimization. Both are single-request, unauth-safe to fingerprint — fits our doctrine directly.



## 2. IDOR/BAC — parameter-pollution + mass-assignment combo bypass, concrete technique for `class-idor.md`

Our IDOR ranker scores object-ref params and ID type but doesn't yet document the two bypass techniques below — both are single-request, GET/PUT-safe to *detect* (own-account only, matches our 2-owned-account doctrine), and both explain WHY an endpoint that looks access-controlled (owner check present) is still exploitable.



Nothing else from this pass cleared the bar for new/actionable (GraphQL, SSRF, SQLi searches this run either matched material we already surfaced in the last 30 days or came back generic/non-actionable).
